#takes 5 tuple from key generator and 4 tuple from offset_generator and
# returns a 4 tuple... somehow.

defmodule RotatorTest do
  use ExUnit.Case

  import Rotator

  test "the truth" do
    assert 1 + 1 == 2
  end
end
